/**
 * Template de configuração — copie este arquivo para `config.js`
 * e preencha com as credenciais reais do seu projeto Supabase.
 *
 * Onde encontrar as chaves:
 *   supabase.com → seu projeto → Settings → API
 *     - Project URL  → SUPABASE_URL
 *     - anon public  → SUPABASE_ANON
 *
 * O arquivo `config.js` está no .gitignore e NÃO deve ser commitado.
 */
window.__CONFIG__ = {
  SUPABASE_URL:  'https://SEU-PROJETO.supabase.co',
  SUPABASE_ANON: 'SUA_CHAVE_ANON_AQUI'
};
